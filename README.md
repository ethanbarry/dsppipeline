# Rust DSP Pipeline

This is our repo for a giga-turbo-optimized digital signal processing pipeline which'll take radio data from the RTL-SDR and channel it (see what I did there?) into some filters. Ideally this results in a "lock" on the target signal along with a strength indicator for it.
